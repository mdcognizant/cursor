# Universal API Bridge v2.0 - Testing Suite Requirements
# Install with: pip install -r requirements_testing.txt

# Core dependencies
asyncio-extensions>=0.1.0
aiohttp>=3.8.0
numpy>=1.21.0
psutil>=5.8.0

# Performance testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-benchmark>=4.0.0

# Data analysis and reporting
statistics>=1.0.3.5  # Built-in, but listed for clarity
matplotlib>=3.5.0  # For potential chart generation
pandas>=1.3.0  # For data analysis

# Optional optimizations (install if available)
# lz4>=4.0.0  # For LZ4 compression testing
# scikit-learn>=1.0.0  # For ML prediction models

# Development and debugging
pydantic>=1.9.0
typing-extensions>=4.0.0

# Logging and monitoring
structlog>=22.1.0

# Note: Some dependencies are optional and will be gracefully handled if not available 