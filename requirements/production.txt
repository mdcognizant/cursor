# Production Deployment Requirements
# Minimal and optimized for production MCP+gRPC deployment

# Include polygon MCP+gRPC requirements
-r polygon_mcp_grpc.txt

# Production server optimizations
gunicorn>=21.0.0
uvloop>=0.19.0  # Faster asyncio event loop

# Monitoring and logging
structlog>=23.0.0
sentry-sdk>=1.38.0

# Performance monitoring
psutil>=5.9.0
memory-profiler>=0.61.0

# Security enhancements
cryptography>=41.0.0
passlib>=1.7.4

# Database connection pooling for production
asyncpg>=0.29.0  # PostgreSQL async driver
redis>=5.0.0     # For caching in production

# Health checks and metrics
healthcheck>=1.3.0

# Production-specific optimizations
orjson>=3.9.0    # Faster JSON processing
msgpack>=1.0.0   # Efficient serialization 